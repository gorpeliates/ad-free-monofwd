MF+AD experiments are from `experiment_all*` directories. 

BP with CNN experiments had to be re-run because of some changes to the CNN architecture that I forgot to do so when I ran the first experiemtns, so BP results are from the `experiment_only_backprop*` directories.

DD experiments are only from `experiment_only_dd*` folders

The number of perturbation direction experiments are in the `experiments_p_20260614*` directories.